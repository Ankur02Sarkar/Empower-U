"use client";
import React from "react";
const Login = () => {
  return (
    <div>
      <h1>Login</h1>
      <input onChange={() => {}} type="email" placeholder="Enter Email" />
      <input onChange={() => {}} type="password" placeholder="Enter Password" />
      <button onClick={() => {}}>SignUp/In</button>
    </div>
  );
};

export default Login;
